<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.oom.masterzuo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <!-- 个推SDK权限配置结束 -->
    <application
        android:name=".app.MasterZuoApplication_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".app.WelcomeActivity_"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppThemeBase">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".app.main.HomeActivity_"
            android:launchMode="singleTop"
            android:theme="@style/AppThemeBase"/>

        <!--    HomePage    -->
        <activity
            android:name=".app.main.homepage.GoodsClassifyActivity_"
            android:launchMode="singleTop"
            android:theme="@style/AppThemeBase"/>

        <!--    MemberCenter    -->
        <activity
            android:name=".app.main.membercenter.LoginActivity_"
            android:launchMode="singleTop"
            android:theme="@style/AppThemeBase"/>

        <!-- Other -->

    </application>
</manifest>